numpy>=1.24
scipy>=1.10
pandas>=2.0
openpyxl>=3.1
# the Murex .xls (BIFF8) writer for the bulk export; pure Python, no build
xlwt>=1.3
# optional: fuller statutory holiday calendars (built-in rules are used otherwise)
# holidays>=0.40
